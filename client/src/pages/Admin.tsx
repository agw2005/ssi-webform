import Primitive from "../components/reusable/Primitive.tsx";

const Admin = () => {
  return (
    <Primitive
      isLoading={[]}
      isErr={[]}
      componentName="Admin.tsx"
      pageTitle="Admin"
    >
      <div>Admin</div>
    </Primitive>
  );
};

export default Admin;
